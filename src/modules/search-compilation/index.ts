/**
 * @todo implement
 * this plugin allows switching between searches instead of compilating them (like New + Suspended + Learning etc)
 * opt in and toggle on every search
 */
class SearchCompilation {}

new SearchCompilation();
